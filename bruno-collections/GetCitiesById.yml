info:
  name: GetCitiesById
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:6000/api/v1/cities/1
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
